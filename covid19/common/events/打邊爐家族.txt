add_namespace = my_namespace

country_event = {
	id = my_namespace.2
	title = my_namespace.2.t
	desc = my_namespace.2.d
	picture = GMX_news_event_打邊爐家族
	trigger = { always = yes }

	option = {
		name = my_namespace.2.a
        add_stability = -0.03
        ai_chance = { base = 75 }
	}
}

on_actions = {
    on_yearly_pulse = {
        effect = {
            if = {
                limit = { date > 2020.2.9 }
                country_event = { id = my_namespace.2}
            }
        }
    }
}
